<!DOCTYPE html>
<html>
  <head>
    <meta content="text/html; charset=UTF-8" http-equiv="Content-Type" />
  </head>
  <body>
    <h1>You have been invited by <%= @question.group.user.pretty_name %> to vote in an group at WeVoteHere.com!</h1>

    <p><%= link_to "Click here to vote", make_question_valid_svcs_path(@question) %></p>

    <h2>Question Information</h2>
    Owner:
    	 <%= link_to @question.group.user.pretty_name, @question.group.user %>
    	<br>
  	Group:
    	<%= link_to @question.group.name, @question.group %>
    	<br>
  	Question:
    	<%= link_to @question.name, @question %>
   	

    <br>
  	<br>
  	
  	<h3>Choices</h3>
  	<br>
  	<% @question.choices.each do |choice| %>
  		<%= choice.name %>
  		<br>
  	<% end %>
  </body>
</html>