<!DOCTYPE html>
<html>
  <head>
    <meta content="text/html; charset=UTF-8" http-equiv="Content-Type" />
  </head>
  <body>
    <h1>You have been invited by <%= @election.group.user.pretty_name %> to vote in an group at <%= url_for "WeVoteHere.com!", root_url %></h1>

    <% if @election.group.privacy %>
      <p>You will use your Secret Voting Code (SVC) to securely vote on this election. Even we do not know your secret voting code; once this email is sent, we delete the association from our memory. Therefore, please don't lose your SVC!</p>
      <p>Your SVC is <%= url_for @svc, vote_url(@svc) %>. You can click it to vote.</p>
      <p>You can also view the status of your vote here: <%= url_for "Status", status_vote_url(@svc) %></p>
    <% else %>
      <p><%= url_for "Click here to vote", make_election_valid_svcs_url(@election) %></p>
    <% end %>

    <h2>Election Information</h2>
    Owner:
    	 <%= url_for @election.group.user.pretty_name, @election.group.user %>
    	<br>
  	Group:
    	<%= url_for @election.group.name, @election.group %>
    	<br>
  	Election:
    	<%= url_for @election.name, @election %>

    <br>
  	<br>
  	
  	<h3>Choices</h3>
  	<%= render @election.choices %>
  </body>
</html>